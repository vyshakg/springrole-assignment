# springrole-assignment

start the project using `yarn install and  yarn start` or `npm install and npm start`

proxy option is added to package.json to overcome a CORS error

what i used :
  - React
  - Axios
  - Styled-Compnents.
